using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class FieldManagerTests
{
    //[UnityTest]
    //public IEnumerator MoveRowsTest()
    //{
    //    GameObject gameObject = new GameObject();
    //    FieldManager.FindField(0).GameObject = gameObject;

    //    FieldManager.MoveRows();
    //    yield return null;

    //    Assert.AreEqual(gameObject, FieldManager.FindField(6).GameObject);
    //}

    //[UnityTest]
    //public IEnumerator FindFieldWitchGameObjectTest()
    //{
    //    List<Field> fields = CreateTable();
    //    int index = 4;
    //    GameObject gameObject = new GameObject();

    //    yield return new WaitForSecondsRealtime(0.1f);
    //    fields[index].GameObject = gameObject;
    //    FieldManager.FindField(index).GameObject = gameObject;
    //    yield return new WaitForSecondsRealtime(0.1f);

    //    Assert.AreEqual(FieldManager.FindField(gameObject), fields[index]);
    //}

    //List<Field> CreateTable()
    //{
    //    List<Field> fields = new List<Field>();
    //    for (int i = 0; i < FieldManager.GetFieldsCount(); i++)
    //    {
    //        fields[i] = FieldManager.FindField(i);
    //    }
    //    return fields;
    //}
}
